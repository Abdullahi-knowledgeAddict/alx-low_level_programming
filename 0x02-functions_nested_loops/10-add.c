/**
 * add - adds two different integers together.
 * @a: an operand.
 * @b: another operand.
 *
 * Return: returns the value of the result.
 */
int add(int a, int b)
{
	int result;

	result = a + b;

	return (result);
}
