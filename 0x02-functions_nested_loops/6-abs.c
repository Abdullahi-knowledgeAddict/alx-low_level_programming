#include "main.h"
/**
 * _abs - prints the absolute value of a parameter(int).
 * @b: the input integer
 *
 * Return: absolute value.
 */
int _abs(int b)
{

	if (b < 0)
		b = (-1 * b);
	return (b);
}
