#include <stdio.h>
/**
 * main - print all letters of the alphabets
 * using for loop.
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int c;

	for (c = 'a'; c < 'z'; c++)
		putchar(c);
	return (0);
}
